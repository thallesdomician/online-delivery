{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "name": "Debug Delivery",
            "processId": "${command:PickProcess}",
            "protocol": "inspector",
            "restart": true
        }
    ]
  }